# An immutable (cannot be changed) collection of ordered items, written with round brackets ().
# Tuple holds coordinates that should never change
coordinates = (40.7128, -74.0060)
print("Tuple:", coordinates)
fruits = ('banana', 'orange', 'mango', 'lemon') # list of fruits or items without a comma in between each becomes the srting not tuple.
print(fruits)
print(type(fruits))

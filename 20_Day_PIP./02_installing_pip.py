# Definition: PIP often comes pre-installed with Python (versions 3.4 and above). You can check if it's installed by running a command in your terminal. If not, you can install it using a Python script or your OS's package manager.

#  Run this in your terminal (not as Python code) to check PIP version
# pip --version
# Definition: Python's built-in module that provides support for working with regular expressions.

# Explanation: You must import it (import re) before using any regex functions. 
# It contains all the methods like match, search, findall, etc.
import re  # This is how you import the module
print(dir(re))  # Shows all functions available in the re module
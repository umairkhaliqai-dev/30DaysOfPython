# Iterates over a sequence (like a list, string, or range).
# Example: Print each fruit in a list
fruits = ["apple", "melon", "cherry"]
for x in fruits: # x is here a variable name already i desided to give each item inside the list  etc..
    print(x)
# Output: apple, banana, cherry

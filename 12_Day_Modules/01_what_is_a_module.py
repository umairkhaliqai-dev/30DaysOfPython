# A module is simply a Python file (.py) containing functions, classes, or variables that you can reuse in other scripts. Think of it as a toolbox you import when needed.
# mymodule.py — this file IS a module
def say_hello(name):
    return f"Hello, {name}!"

PI = 3.14159   # variables are also part of a module

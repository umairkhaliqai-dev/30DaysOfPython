# A function is a reusable block of code. Use the def keyword to define one.
def greet():
    print("Hello, World!")

# Function is defined but not yet run
def add_two_numbers ():
    num_one = 2
    num_two = 3
    total = num_one + num_two
    print(total)
add_two_numbers()

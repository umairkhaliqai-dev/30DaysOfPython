# Definition: Once activated, any pip install command will install packages
#  only inside that active virtual environment,
#  keeping them separate from other projects and the global Python installation.

# Install a specific version of a package
# pip install requests==2.28.1

# Install the latest version of a package
# pip install pandas

# See all packages installed in this environment only
# pip list